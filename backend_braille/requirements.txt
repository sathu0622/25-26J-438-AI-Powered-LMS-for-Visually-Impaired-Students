fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
torch>=2.5.0
transformers>=4.35.0
peft>=0.7.0
sentence-transformers>=2.2.2
scikit-learn>=1.3.0
accelerate>=0.25.0
huggingface-hub>=0.19.0
python-multipart>=0.0.6
# dotenv support for environment variables (HF_TOKEN etc.)
python-dotenv>=1.0.0
huggingface_hub

# Image / PDF processing libraries used by braille_decoder.py
#   opencv for contour detection, numpy for arrays,
#   Pillow for image handling, pymupdf (imported as fitz) for PDF reading
opencv-python>=4.8.0
numpy>=1.26.0
Pillow>=10.0.0
pymupdf>=1.23.0

# Note: bitsandbytes is not available on Windows
# The code will automatically work without it (no quantization)
# For Linux/WSL, you can optionally install: pip install bitsandbytes

chromadb
pandas
openpyxl