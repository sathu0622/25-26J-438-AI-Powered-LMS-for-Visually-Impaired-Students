# Backend Server Configuration
HOST=127.0.0.1
PORT=8003
RELOAD=false

# API Base URL
API_BASE_URL=http://localhost:8004

# Simulated generation delay for sample audio responses (seconds, max 10)
AUDIO_GENERATION_TIME=10
